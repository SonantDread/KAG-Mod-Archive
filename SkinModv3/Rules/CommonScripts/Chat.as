//made by vamist.. is a very bad script, send help.
/*void onCommand(CPlayer@ this)
{
	string username = this.getUsername();

	if (username == "Vamist")
	{
		client_AddToChat("Hi");
		//getNet().server_SendMsg("hi"); //does not work yet. at least it does not break! :) (still unsure what to do with this script..)
	}
}

*/